// src/hooks/useCarouselSync.js
// No-op stub — carousel sync removed (no backend).
export function useCarouselSync() {
  return { saveStatus: 'idle', carouselId: null, setCarouselId: () => {}, saveCount: 0, saveNow: () => {} }
}
